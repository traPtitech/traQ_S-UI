// @ts-check
;(() => {
  /**
   * @type {import('../config').Config}
   */
  const config = {
    enableQall: true,
    enableSearch: true,
    isRootChannelSelectableAsParentChannel: false
  }

  self.traQConfig = config
})()
